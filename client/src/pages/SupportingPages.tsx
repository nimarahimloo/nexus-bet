import { Link } from "wouter";
import { useState } from "react";
import { ArrowDownLeft, ArrowLeft, ArrowUpRight, BrainCircuit, Check, Crown, Gift, LockKeyhole, ShieldCheck, Sparkles, WalletCards } from "lucide-react";
import { PageShell } from "@/components/PageShell";
import { formatFaDecimal } from "@shared/format";
import type { AiPick } from "@shared/ai";
import { trpc } from "@/lib/trpc";
import { useAuth } from "@/_core/hooks/useAuth";

const usdt = (value: number) => formatFaDecimal(value);

export function WalletPage() { const { isAuthenticated } = useAuth(); const [amount, setAmount] = useState(""); const walletQuery = trpc.wallet.me.useQuery(undefined, { enabled: isAuthenticated }); const transactionsQuery = trpc.wallet.transactions.useQuery(undefined, { enabled: isAuthenticated }); const requestMutation = trpc.wallet.request.useMutation({ onSuccess: () => { setAmount(""); void transactionsQuery.refetch(); } }); const wallet = walletQuery.data; const submitRequest = (type: "deposit" | "withdrawal") => { const value = Number(amount); if (Number.isFinite(value) && value > 0) requestMutation.mutate({ type, amount: value, network: "TRC-20" }); }; return <PageShell eyebrow="کیف پول USDT" title="دارایی‌ات، روشن و تحت کنترل" description="موجودی و درخواست‌های کیف پول از backend خوانده می‌شوند؛ هر واریز یا برداشت تا تأیید provider در وضعیت pending می‌ماند." heroImage="/manus-storage/nexus-bet-wallet-hero-v2_884a12f2.png"><div className="balance-hero glass-panel"><span>موجودی قابل‌استفاده</span><strong>{walletQuery.isLoading ? "در حال دریافت…" : wallet ? usdt(wallet.availableBalance) : "—"} <small>USDT</small></strong><div><span>قفل‌شده در شرط‌های باز</span><b>{wallet ? usdt(wallet.lockedBalance) : "—"} USDT</b></div>{!isAuthenticated && <p className="data-state">برای مشاهدهٔ موجودی واقعی وارد حساب شوید.</p>}{walletQuery.error && <p className="data-state error">موجودی واقعی در دسترس نیست.</p>}</div>{isAuthenticated && <div className="standalone-card glass-panel wallet-request"><label><span>مبلغ USDT</span><input value={amount} onChange={(event) => setAmount(event.target.value)} inputMode="decimal" placeholder="مثلاً ۱۰" /></label><div className="wallet-request-actions"><button className="solid-cta" disabled={requestMutation.isPending} onClick={() => submitRequest("deposit")}><ArrowDownLeft size={16} /> ثبت درخواست واریز</button><button className="outline-cta" disabled={requestMutation.isPending} onClick={() => submitRequest("withdrawal")}><ArrowUpRight size={16} /> ثبت درخواست برداشت</button></div>{requestMutation.error && <p className="inline-alert">درخواست ثبت نشد؛ مقدار و اطلاعات را بررسی کن.</p>}</div>}<div className="standalone-grid two"><article className="standalone-card glass-panel"><ArrowDownLeft /><h3>واریز</h3><p>درخواست به‌صورت pending ثبت می‌شود و پس از اتصال provider بلاکچین تأیید خواهد شد.</p></article><article className="standalone-card glass-panel"><ArrowUpRight /><h3>برداشت</h3><p>درخواست در backend ثبت می‌شود و قبل از ارسال به شبکه نیازمند بررسی امنیتی است.</p></article></div><div className="standalone-card glass-panel"><h3>تاریخچه تراکنش‌ها</h3>{!isAuthenticated ? <p className="data-state">برای مشاهدهٔ تاریخچه وارد حساب شوید.</p> : transactionsQuery.isLoading ? <p className="data-state">در حال دریافت…</p> : transactionsQuery.data?.length ? transactionsQuery.data.map((tx) => <div className="reward-history-row" key={tx.id}><WalletCards size={16} /><div><b>{tx.type}</b><span>{new Date(tx.createdAt).toLocaleString("fa-IR")}</span></div><strong>{usdt(tx.amount)} USDT · {tx.status}</strong></div>) : <p className="data-state">هنوز تراکنشی ثبت نشده است.</p>}</div></PageShell>; }

export function AiPage() { const sportsQuery = trpc.sports.fixtures.useQuery({ next: 10 }, { staleTime: 30_000 }); const candidates = (sportsQuery.data?.matches ?? []).flatMap((match) => match.markets.filter((market) => market.odds > 0).map((market, index) => ({ eventId: match.id, league: match.league, match: `${match.home} — ${match.away}`, sport: match.sport, marketLabel: market.label, marketName: market.name, odds: market.odds, status: match.status, popularity: Math.max(50, 90 - index * 8) }))); const aiQuery = trpc.ai.smartPicks.useQuery({ candidates }, { enabled: candidates.length > 0, staleTime: 60_000 }); return <PageShell eyebrow="NEXUS AI" title="پیشنهادها را بهتر بفهم" description="سیگنال‌های توضیح‌پذیر بر اساس فید مسابقات و بازارهای موجود؛ نه توصیهٔ قطعی و نه وعدهٔ سود." heroImage="/manus-storage/nexus-bet-ai-hero-v2_ad9c88ae.png"><div className="ai-callout glass-panel"><BrainCircuit size={23} /><div><b>تحلیل متصل به فید مسابقات</b><p>{sportsQuery.isLoading ? "در حال دریافت فید واقعی مسابقات…" : aiQuery.isLoading ? "در حال تحلیل بازارهای موجود…" : "پیشنهادها از دادهٔ فید فعلی ساخته می‌شوند."}</p></div><span className="sample-chip">غیرتضمینی</span></div>{sportsQuery.error || sportsQuery.data?.error ? <div className="inline-alert">فید واقعی در دسترس نیست؛ برای جلوگیری از ساخت پیشنهاد جعلی، نتیجه‌ای نمایش داده نمی‌شود.</div> : null}<div className="standalone-grid">{aiQuery.data?.picks.map((pick: AiPick) => <article className="ai-pick-card glass-panel" key={`${pick.eventId}-${pick.marketLabel}`}><div><Sparkles size={16} /><span>{pick.tags.join(" · ") || "تحلیل الگوریتمی"}</span></div><h3>{pick.marketName}</h3><p>{pick.match}</p><strong>{formatFaDecimal(pick.odds)}</strong><span className="risk-chip">ریسک {pick.risk} · اعتماد {formatFaDecimal(pick.confidence, 0)}٪</span><p>{pick.rationale}</p><button className="outline-cta" disabled>افزودن به بلیت <ArrowLeft size={15} /></button></article>)}</div>{!aiQuery.isLoading && !aiQuery.data?.picks.length && !sportsQuery.isLoading && <div className="empty-state glass-panel">برای این لحظه پیشنهاد قابل اتکایی از فید موجود تولید نشد.</div>}<p className="data-state">منبع: {aiQuery.data?.source === "ai" ? "Nexus AI روی فید مسابقات" : "فید خالی یا سرویس AI در دسترس نیست"}. نتیجه تضمین‌شده نیست.</p></PageShell>; }

export function VipPage() { const { isAuthenticated } = useAuth(); const summaryQuery = trpc.vip.summary.useQuery(undefined, { enabled: isAuthenticated }); const summary = summaryQuery.data; return <PageShell eyebrow="NEXUS VIP" title="فعال بمان، بیشتر دیده شو" description="سطح و progress از activity ledger واقعی محاسبه می‌شوند؛ هیچ امتیاز یا tier ساختگی در رابط نمایش داده نمی‌شود." heroImage="/manus-storage/nexus-bet-vip-hero-v2_6e37762c.png"><div className="vip-level glass-panel"><div><Crown size={24} /><span>وضعیت باشگاه</span><strong>{summaryQuery.isLoading ? "در حال دریافت…" : summary?.currentTier ?? "—"}</strong></div><div><b>{summary ? formatFaDecimal(summary.points, 0) : "—"}</b><span>امتیاز فعالیت</span></div><div className="vip-progress"><span style={{ width: `${summary?.progress ?? 0}%` }} /><small>{summary?.nextTier ? `${formatFaDecimal(summary.nextThreshold ?? 0, 0)} امتیاز تا ${summary.nextTier}` : isAuthenticated ? "بیشترین سطح فعال" : "برای مشاهده وارد شوید"}</small></div></div>{!isAuthenticated && <div className="data-state preview-notice"><ShieldCheck size={15} /><span>برای دیدن tier و امتیاز واقعی وارد حساب شوید.</span></div>}{summaryQuery.error && <div className="inline-alert">activity ledger فعلاً در دسترس نیست.</div>}<div className="standalone-grid two"><article className="standalone-card glass-panel"><Gift size={21} /><h3>مزایای فعال</h3><p>مزایا فقط بعد از ثبت در policy و activity ledger نمایش داده می‌شوند.</p></article><article className="standalone-card glass-panel"><WalletCards size={21} /><h3>اولویت خدمات کیف پول</h3><p>وضعیت سرویس بر اساس tier واقعی حساب تعیین خواهد شد.</p></article></div></PageShell>; }

export function AccountPage() { const { user, isAuthenticated } = useAuth(); const overviewQuery = trpc.account.overview.useQuery(undefined, { enabled: isAuthenticated }); const overview = overviewQuery.data; const bets = overview?.bets ?? []; const openBets = bets.filter((bet) => bet.status === "pending"); return <PageShell eyebrow="حساب کاربری" title="همه‌چیز دربارهٔ حساب تو" description="شرط‌ها، spinها و تراکنش‌های کیف پول از یک overview backend خوانده می‌شوند؛ هیچ رکورد نمایشی جای فعالیت واقعی را نمی‌گیرد." heroImage="/manus-storage/nexus-bet-account-hero-v2_f471d0ea.png"><div className="account-summary glass-panel"><div className="avatar">N</div><div><b>{user?.name ?? "حساب Nexus"}</b><span>{isAuthenticated ? "فعالیت حساب از منبع واقعی" : "برای مشاهدهٔ فعالیت وارد حساب شوید."}</span></div><ShieldCheck size={21} /></div><div className="standalone-grid two"><article className="standalone-card glass-panel"><FileClockIcon /><h3>شرط‌های باز</h3><strong>{isAuthenticated ? `${openBets.length} بلیت فعال` : "—"}</strong>{openBets[0] ? <p>{openBets[0].selections[0]?.match ?? "انتخاب ثبت‌شده"} · ضریب {formatFaDecimal(openBets[0].combinedOdds)}</p> : <p>{overviewQuery.isLoading ? "در حال دریافت…" : "هنوز بلیت ثبت‌شده‌ای وجود ندارد."}</p>}</article><article className="standalone-card glass-panel"><Gift /><h3>فعالیت پاداش</h3><strong>{isAuthenticated ? `${overview?.wheelSpins.length ?? 0} spin` : "—"}</strong><p>تاریخچهٔ reward از backend به‌روزرسانی می‌شود.</p></article></div>{overviewQuery.error && <div className="inline-alert">اطلاعات حساب فعلاً از backend دریافت نشد.</div>}</PageShell>; }
function FileClockIcon() { return <LockKeyhole size={21} />; }
